// 导入依赖
var React = require('react');
var ReactDOM = require('react-dom');


// 在React的主入口文件中添加下列内容
var reactElement = React.createElement('h1',
	{className: 'header'},
	'This is React Element of Content');
ReactDOM.render(reactElement,
	document.getElementById('react-application')
);